<!-- USERS CAMPING LIST -->
<div class="container">

  <div id="camping_list">
    <% @user.camping_lists.each do |p| %>
    <li><%= "#{p.item}"%></li>
    <% end %>
  </div>

  <%= form_for @camping_lists do |f| %>
    <%= f.label :item %>:
    <%= f.submit %>
    <% end %>


</div>
